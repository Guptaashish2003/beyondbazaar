/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '',
    images: {
      domains: ['source.unsplash.com','www.example.com'],
    },
    swcMinify: true,
}

  
module.exports = nextConfig
