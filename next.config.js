/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '',
    images: {
      domains: ['images.unsplash.com','www.example.com'],
    },
    swcMinify: true,
}

  
module.exports = nextConfig
